<!DOCTYPE html>
<html>
    <head>
        <title>VenixDev Image Server</title>

        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta property="og:url" content="https://i.venixdev.xyz/<%= fileName %>">
        <meta property="og:title" content="VenixDev CDN">
        <meta name="theme-color" content="#008B8B">
        <% if (format === 'mp4') { %>
            <meta property="og:type" content="video.other">
            <meta property='og:video:url' content="https://i.venixdev.xyz/raw/<%= fileName %>" />
            <meta property='og:video:secure_url' content="https://i.venixdev.xyz/raw/<%= fileName %>" />
            <meta property="og:video:type" content="text/html">
            <meta property="og:video:width" content="1280" />
            <meta property="og:video:height" content="720" />
        <% } else { %>
            <meta property="og:type" content="article">
            <meta property="og:image" content="https://i.venixdev.xyz/raw/<%= fileName %>">
            <meta property="og:image:width" content="<%= imgSize.width %>" />
            <meta property="og:image:height" content="<%= imgSize.height %>" />
            <meta name="twitter:card" content="summary_large_image" />
        <% } %>
        <meta name="author" content="VenixDev#3371" />
        <% if (format === 'mp4') { %>
        <% } else { %>
           <meta name="twitter:card" content="summary_large_image" />
           <meta property="twitter:image" content="https://i.venixdev.xyz/raw/<%= fileName %>" />
        <% } %>
        <style>
            @import url('https://fonts.googleapis.com/css2?family=Cabin:wght@500&display=swap');

            body, html {
                font-family: "Cabin", sans-serif;
                background-color: #181818;
                color: white;
                overflow: hidden;
                margin: 0 auto;
            }

            .centerArea {
                display: flex;
                align-items: center;
                min-height: 100vh;
            }

            .textArea {
                color: #fff;
                width: 100%;
                text-align: center;
            }
        </style>
    </head>
    <body>
        <div class="centerArea">
            <div class="textArea">
                <h1>VenixDev CDN</h1>
                <% if (format === 'mp4') { %>
                    <embed src="/raw/<%= fileName %>" autostart="false" height="<%= imgSize.height %>" width="<%= imgSize.width %>" /></embed>
                <% } else { %>
                    <img src="/raw/<%= fileName %>" >
                <% } %>
            </div>
        </div>
    </body>
</html>